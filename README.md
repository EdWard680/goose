# goose
duck, duck

```
docker stop <container name>
docker rm <container name>
docker -H <host> build -t <version tag> .
docker -H <host> run -[d]it --privileged --network=host --name -v /data:/data
```